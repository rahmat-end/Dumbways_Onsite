import * as amqp from "amqplib"
import { EventEmitter } from "stream"

export default new class ThreadWorker extends EventEmitter {
    private readonly 
    async add(queueName: string, connect: amqp.Connection) {
        try {
            
        } catch (error) {
            
        }
    }
}